spring:
  datasource:
    username: root
    password: test1234
    url: jdbc:mysql://localhost:3306/board
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: create
logging:
  level:
    org.hibernate.orm.jdbc.bind: trace